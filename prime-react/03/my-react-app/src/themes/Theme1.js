import { createTheme } from "@mui/material/styles";

export const Theme1 = createTheme({
  palette: {
    primary: { main: "#00f", light: "#00f" },
    secondary: { main: "#0f0", light: "#0f0" },
    otherColor: { main: "#f00", light: "#f00" },
  },
});
