import { createTheme } from "@mui/material/styles";

export const Theme1 = createTheme({
  palette: {
    primary1: { main: "#00f", light: "#00f" },
    secondary1: { main: "#00a", light: "#00a" },
    otherColor: { main: "#f00", light: "#f00" },
  },
});
